# MCP Architecture

The previous source-specific MCP design has been removed.

Future MCP integration should focus on reading Pencil selections and producing `UguiNode` output without requiring Unity editor interaction.
