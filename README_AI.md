# AI Workflow

All legacy source-specific AI workflow has been removed.

The repository is now prepared for a new source pipeline, such as `pencil2ugui`, where AI can:

- read design data outside Unity
- map design nodes into `UguiNode`
- hand off the result to the Unity conversion layer
